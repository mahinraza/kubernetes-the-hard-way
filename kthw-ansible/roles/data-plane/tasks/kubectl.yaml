---
- name: Installing kubectl...
  ansible.builtin.get_url:
    url: "https://dl.k8s.io/release/{{ kubectl.version }}/bin/linux/{{ ansible_env.ARCH }}/kubectl"
    dest: /usr/local/bin/kubectl
    mode: '0755'