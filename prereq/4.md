### You can create the virtual environment(optional)
```bash
sudo apt install python3-virtualenv -y

virtualenv ~/kubernetes-the-hard-way/kthw-ansible/venv

source ~/kubernetes-the-hard-way/kthw-ansible/venv/bin/activate

python3 -m pip install ansible
```